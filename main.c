#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

#include "funcionario.h"
#include "buscas_sequenciais.h"
#include "buscas_binarias.h"
#include "livro.h"

/*int main(int argc, char *argv[])    //Para compilar use ->  gcc -o app ./*.c
{
    printf("\033[H\033[J"); //Limpar tela do seu terminal
    FILE *employeeFile, *bookFile, *logFile, *logFileBinary, *employeeFileBinary, *bookFileBinary;
    printf("Buscas Sequenciais\n");
    bases_buscas_sequenciais(employeeFile, bookFile, logFile, 1000);

    system("pause");
    
    int tamanho_base = 10000; // Escolha o tamanho da base desejada
    bases_buscas_binarias(employeeFileBinary, bookFileBinary, logFileBinary, tamanho_base);

    fclose(employeeFileBinary);
    fclose(bookFileBinary);
    fclose(logFileBinary);
    return 0;
}
*/



